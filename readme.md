# TO DO LATER

# Configuring the .lon file
The .lon file containing the map and creature data must end with a blank line. The module **blessed** starts at the coordinate (0, 0). Thus we consider the first line as the 0th line. Same for columns.